import { Command } from '../../../types';

const config: Command = {
    description: 'Lista branch NON mergiate',
    longHelp: 'Lista branch NON mergiate nel repository corrente',
    exec: './exec',
    params: []
};

export default config;