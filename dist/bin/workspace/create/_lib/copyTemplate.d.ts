import { GenericObject } from '../../../../types';
/**
 * Render and copy all the ./template files, except some one that are managed manually
 * @param answers
 */
export declare function copyTemplate(answers: GenericObject): Promise<void>;
