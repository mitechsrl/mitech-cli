import { GenericObject } from '../../../../types';
/**
 * Creates a workspace package.json file in the current working directory
 * @param answers
 */
export declare function packageJsonBuilder(answers: GenericObject): void;
